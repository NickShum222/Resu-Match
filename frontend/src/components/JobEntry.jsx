import React from 'react'

const JobEntry = ({id, title, company, status, date_applied, resume_id}) => {
  
  return (
    <div className='w-full flex justify-between items-center'>

    </div>
  )
}

export default JobEntry